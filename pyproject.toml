[project]
name = "h3-scribe"
version = "0.6.4"
description = "MiniMax H3 prompt authoring for ComfyUI"
requires-python = ">=3.10"
dependencies = ["pydantic>=2.7"]

# Choose the project license and fill Registry publisher metadata before publication.
# Fill PublisherId / DisplayName / Icon after reserving the real Comfy Registry publisher.
# [tool.comfy]
# PublisherId = "..."
# DisplayName = "H3 Scribe"
# Icon = ""
